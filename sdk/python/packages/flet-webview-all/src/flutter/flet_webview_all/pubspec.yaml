name: flet_webview_all
description: Flet FletWebviewAll control - A unified webview control for displaying web content across all platforms
version: 0.1.8
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flet: ^0.86.5
  webview_all: ^1.3.3
  webview_all_windows: ^1.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
