import 'package:webview_flutter/webview_flutter.dart';

/// Web stub for [allowFileAccess].
///
/// `webview_flutter_android` imports `dart:io` and therefore cannot be
/// compiled for the web, so it is conditionally swapped out for this no-op.
Future<void> allowFileAccess(WebViewController controller) async {}
